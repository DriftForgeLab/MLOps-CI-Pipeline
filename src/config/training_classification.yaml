# Be aware that this YAML file may need to change, as the classification training code is not yet implemented.
model:
  algorithm: random_forest      # Valid: random_forest | logistic_regression

  hyperparameters:
    n_estimators: 100           
    max_depth: null             
    min_samples_split: 2        
    class_weight: balanced      